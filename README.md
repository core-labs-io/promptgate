# promptgate

*Every prompt you send is too expensive and too revealing. Fix both with one line.*

(Full documentation is written once the implementation is complete — see `ProjectRequirements.txt` for the spec this library implements.)
